name: api_client
description: Client for the Compass API.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  json_annotation: ^4.9.0
  logging: ^1.3.0
  shared_preferences: ^2.4.0

dev_dependencies:
  build_runner: ^2.4.14
  json_serializable: ^6.9.2
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^7.0.0
