name: api_client
description: Client for the Compass API.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.5
resolution: workspace

dependencies:
  logging: ^1.3.0
  models: ^0.1.0
  shared_preferences: ^2.5.5

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.30.0
