name: api
description: A Dart Frog API for the Compass server.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.7.0 <4.0.0"
resolution: workspace

dependencies:
  dart_frog: ^1.2.0
  models: ^0.1.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.15
  very_good_analysis: ^7.0.0
